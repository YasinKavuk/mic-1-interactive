import { InterruptVisualizationComponent } from './interrupt-visualization.component'

describe('InterruptVisualizationComponent', () => {
  it('should mount', () => {
    cy.mount(InterruptVisualizationComponent)
  })
})